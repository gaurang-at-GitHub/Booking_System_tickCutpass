services:
  postgresdb:
    image: postgres:17
    ports:
     - '5433:5432'
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
     - "tickCutpass:/var/lib/postgresql/data"

volumes:
  tickCutpass:     

       